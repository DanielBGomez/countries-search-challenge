// Modules
import React from 'react';
import { render } from 'react-dom';

// Modules
import App from './modules/App';

//
const DOM_ELEMENT = document.getElementById('app');

// Render
render(<App />, DOM_ELEMENT);
